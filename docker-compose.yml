services:
  identity-mysql:
    image: mysql:8.4
    container_name: nexus-identity-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: 123123
      MYSQL_DATABASE: nexus_identity_db
    ports:
      - "3307:3306"
    volumes:
      - identity-mysql-data:/var/lib/mysql

  work-mysql:
    image: mysql:8.4
    container_name: nexus-work-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: 123123
      MYSQL_DATABASE: nexus_work_db
    ports:
      - "3308:3306"
    volumes:
      - work-mysql-data:/var/lib/mysql

  document-mysql:
    image: mysql:8.4
    container_name: nexus-document-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: 123123
      MYSQL_DATABASE: nexus_document_db
    ports:
      - "3309:3306"
    volumes:
      - document-mysql-data:/var/lib/mysql

  ingestion-mysql:
    image: mysql:8.4
    container_name: nexus-ingestion-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: 123123
      MYSQL_DATABASE: nexus_ingestion_db
    ports:
      - "3310:3306"
    volumes:
      - ingestion-mysql-data:/var/lib/mysql

  redis:
    image: redis:7.4-alpine
    container_name: nexus-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis-data:/data

  kafka:
    image: apache/kafka:latest
    container_name: nexus-kafka
    restart: unless-stopped
    ports:
      - "9092:9092"
    environment:
      # Kafka chay KRaft single-node theo chuan Apache image
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://:29092,PLAINTEXT_HOST://:9092,CONTROLLER://:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
    volumes:
      - kafka-data:/var/lib/kafka/data

  qdrant:
    image: qdrant/qdrant:v1.12.6
    container_name: nexus-qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant-data:/qdrant/storage

  chat-service:
    build:
      context: ./chat-service
    container_name: nexus-chat-service
    restart: unless-stopped
    env_file:
      - ./chat-service/.env
    environment:
      CHAT_SERVICE_PORT: 8090
      JWT_SIGNER_KEY: ${JWT_SIGNER_KEY:-44b63ac98e12ce6ca2979e22a56fff7a7a7fea0b4c174965105f52f9d0c278ff}
      WORK_SERVICE_BASE_URL: ${WORK_SERVICE_BASE_URL:-http://work-service:8082}
      WORK_SERVICE_TIMEOUT_SECONDS: 5
    ports:
      - "8090:8090"
    depends_on:
      - work-service

  discovery-server:
    build:
      context: .
      dockerfile: Dockerfile.java
      args:
        MODULE: discovery-server
    container_name: nexus-discovery-server
    restart: unless-stopped
    ports:
      - "8761:8761"

  identity-service:
    build:
      context: .
      dockerfile: Dockerfile.java
      args:
        MODULE: identity-service
    container_name: nexus-identity-service
    restart: unless-stopped
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://identity-mysql:3306/nexus_identity_db
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: 123123
      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://discovery-server:8761/eureka/
    ports:
      - "8081:8081"
    depends_on:
      - discovery-server
      - identity-mysql
      - kafka

  work-service:
    build:
      context: .
      dockerfile: Dockerfile.java
      args:
        MODULE: work-service
    container_name: nexus-work-service
    restart: unless-stopped
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://work-mysql:3306/nexus_work_db
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: 123123
      SPRING_DATA_REDIS_HOST: redis
      SPRING_DATA_REDIS_PORT: 6379
      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://discovery-server:8761/eureka/
    ports:
      - "8082:8082"
    depends_on:
      - discovery-server
      - work-mysql
      - redis
      - kafka

  document-service:
    build:
      context: .
      dockerfile: Dockerfile.java
      args:
        MODULE: document-service
    container_name: nexus-document-service
    restart: unless-stopped
    env_file:
      - ./document-service/.env
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://document-mysql:3306/nexus_document_db
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: 123123
      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://discovery-server:8761/eureka/
      R2_ENDPOINT: ${R2_ENDPOINT:-}
      R2_ACCESS_KEY: ${R2_ACCESS_KEY:-}
      R2_SECRET_KEY: ${R2_SECRET_KEY:-}
      R2_BUCKET: ${R2_BUCKET:-}
      R2_REGION: ${R2_REGION:-auto}
      JWT_SIGNER_KEY: ${JWT_SIGNER_KEY:-44b63ac98e12ce6ca2979e22a56fff7a7a7fea0b4c174965105f52f9d0c278ff}
      INTERNAL_SERVICE_TOKEN: ${INTERNAL_SERVICE_TOKEN:-change-me-internal-token}
    ports:
      - "8083:8083"
    depends_on:
      - discovery-server
      - document-mysql
      - kafka

  ingestion-service:
    build:
      context: ./ingestion-service
    container_name: nexus-ingestion-service
    restart: unless-stopped
    environment:
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      DOCUMENT_SERVICE_BASE_URL: http://document-service:8083
      INTERNAL_SERVICE_TOKEN: ${INTERNAL_SERVICE_TOKEN:-change-me-internal-token}
      INGESTION_MYSQL_URL: mysql+pymysql://root:123123@ingestion-mysql:3306/nexus_ingestion_db
      QDRANT_URL: http://qdrant:6333
      R2_ENDPOINT: ${R2_ENDPOINT:-}
      R2_ACCESS_KEY: ${R2_ACCESS_KEY:-}
      R2_SECRET_KEY: ${R2_SECRET_KEY:-}
      R2_BUCKET: ${R2_BUCKET:-}
      R2_REGION: ${R2_REGION:-auto}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      EMBEDDING_DIMENSION: ${EMBEDDING_DIMENSION:-1536}
    depends_on:
      - document-service
      - ingestion-mysql
      - kafka
      - qdrant

  api-gateway:
    build:
      context: .
      dockerfile: Dockerfile.java
      args:
        MODULE: api-gateway
    container_name: nexus-api-gateway
    restart: unless-stopped
    environment:
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://discovery-server:8761/eureka/
      CHAT_SERVICE_URI: http://chat-service:8090
    ports:
      - "8080:8080"
    depends_on:
      - discovery-server
      - identity-service
      - work-service
      - document-service
      - chat-service

  front-end:
    build:
      context: ./front-end
    container_name: nexus-front-end
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 5137
      API_GATEWAY_URL: http://api-gateway:8080
    ports:
      - "5137:5137"
    depends_on:
      - api-gateway

volumes:
  identity-mysql-data:
  work-mysql-data:
  document-mysql-data:
  ingestion-mysql-data:
  redis-data:
  kafka-data:
  qdrant-data:
